# Copy this file to `.credentials` and populate with values from Google Cloud
# Console (APIs & Services → Credentials → OAuth 2.0 Client IDs, "Desktop app"
# type). `.credentials` is gitignored and must never be committed.
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
